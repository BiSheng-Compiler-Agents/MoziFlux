name: CI

on:
  workflow_dispatch:
  merge_group:
    branches: [main]
    types: [checks_requested]
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  pre-commit:
    uses: ./.github/workflows/pre-commit.yml
